# ML training stack for the starling transfer-classification model.
# Target env: conda `openrlhf` (torch 2.10+cu128, transformers 5.7, accelerate already present).
# Only sentence-transformers is typically missing there; the rest are pinned loosely for reference.
torch>=2.4
transformers>=4.40
accelerate>=0.30
sentence-transformers>=2.7
datasets>=2.18
pyarrow>=15
numpy>=1.26
scikit-learn>=1.3
pyyaml>=6.0
wandb>=0.16        # experiment tracking (report_to: wandb)
# MolFormer-XL custom modeling code (loaded via trust_remote_code) needs these:
einops>=0.7
